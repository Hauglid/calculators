name: calculators
description: App for different types of calculators.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: 5.0.1 # Business logic
  flutter_cubit: 0.1.0
  math_expressions: 2.0.0 # Used to evaluate mathematical strings

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
