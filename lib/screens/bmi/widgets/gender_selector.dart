import 'package:flutter/material.dart';

class GenderSelector extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('TODO gender selector'),
    );
  }
}
